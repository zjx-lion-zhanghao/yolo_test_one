# Artifacts

This directory is reserved for generated outputs such as YOLO training runs,
metrics, plots, validation images, and checkpoints.

Runtime outputs are intentionally ignored by Git. The standard training entry
point writes to:

```text
artifacts/runs/
```
