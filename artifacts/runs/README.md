# YOLO Runs

Training runs created by `python -m yolo_test.training.train_yolo` are written
here by default.

Typical generated files include:

- `weights/best.pt`
- `weights/last.pt`
- `results.csv`
- `results.png`
- precision/recall/F1 curves
- confusion matrices

These generated files are ignored by Git.
