# Trained Models

Put exported or selected project-trained weights here, for example:

- `circle-yolov8n-v1.pt`
- `middle-yolov8m-v1.pt`

Model binaries are intentionally ignored by Git. Keep reproducible training
settings in `configs/` and publish important weights as release assets.
